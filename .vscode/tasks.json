{
    "tasks": [
        {
            "type": "shell",
            "label": "compile",
            // "command": "rm -rf build && meson build --buildtype release && ninja -C build"
            "command": "rm -rf build && meson build && ninja -C build"
        }
    ]
}